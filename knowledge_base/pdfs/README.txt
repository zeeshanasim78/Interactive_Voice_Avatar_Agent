Placeholder knowledge-base PDFs for Phase 5 (Section 5.5) development and
testing, in the same spirit as the placeholder `file:` entries in
config/video_map.yaml (Section 2.3 Assumptions: NAB will supply the real
vetted public PDFs; NFR1 requires all knowledge sources to be pre-vetted
public documents only).

Swap these out for NAB-approved brochures/annual reports/FAQs with no code
change required (NFR6) -- just replace the files here and re-run
`python -m src.rag.ingest`.
